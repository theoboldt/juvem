{% extends 'base.html.twig' %}

{% block title %}Teilnehmer-Herkunft ({{ event.title }}){% endblock %}

{% block body %}
  {% embed 'common/header/base-page-header.html.twig' with {'title': 'Teilnehmer-Herkunft', 'subtitle': event.title} %}
    {% block breadcrumb %}
      <li><a href="{{ path('event_list') }}">Veranstaltungen</a></li>
      <li><a href="{{ path('event', {'eid': event.eid }) }}">{{ event.title }}</a></li>
      <li class="active">Teilnehmer-Herkunft</li>
    {% endblock %}
  {% endembed %}
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <p>Die folgende Tabelle gibt Aufschluss darüber, woher die bestätigten Teilnehmer stammen.</p>
      </div>
      <div class="col-xs-12" id="participantsLocationDistribution"
           data-url="{{ url('event_participants_location_data', {'eid': event.eid}) }}">
        <div class="btn-toolbar" role="toolbar">
          <div class="btn-group" data-toggle="buttons">
            <label class="btn btn-default active disabled" id="detail-rough">
              <input type="radio" name="detail" id="rough" checked> Grob
            </label>
            <label class="btn btn-default disabled" id="detail-detailed">
              <input type="radio" name="detail" id="detailed"> Fein
            </label>
          </div>
        </div>
        <table class="table table-striped table-condensed">
          <thead>
          <tr>
            <td>Ort</td>
            <td class="number">Teilnehmer</td>
            <td></td>
          </tr>
          </thead>
          <tbody>
          <tr>
            <td class="loading-text" colspan="3">
              <i>Die Anmeldungsdaten werden untersucht, die Herkunftsverteilung wird vorbereitet...</i>
            </td>
          </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
{% endblock %}