{% extends 'base.html.twig' %}
{% import _self as m %}

{% block title %}Anmelden bei {{ event.title }}{% endblock %}

{% block body %}
    <div class="container page-participate">
    <div class="row">
        <div class="col-xs-12 col-sm-offset-1 col-sm-10 col-lg-offset-2 col-lg-8">
            <div class="row">
                <div class="col-xs-12">
                    <ol class="breadcrumb">
                        <li><a href="{{ path('homepage') }}">Juvem</a></li>
                        <li><a href="{{ path('event_public_detail', {'eid': event.eid }) }}">{{ event.title }}</a></li>
                        <li class="active">Anmeldung überprüfen (2/3)</li>
                    </ol>
                    {% include 'common/header/inner-page-header.html.twig' with {'title': 'Anmeldung überprüfen (2/3)', 'subtitle': event.title} %}
                </div>
            </div>
            <div class="row">
                <div class="col-xs-12">
                    <div class="alert alert-success" role="alert">
                        Wir haben Ihren Teilnahmewunsch festgehalten. Sie erhalten eine automatische Bestätigung, dass die Anfrage bei uns eingegangen ist.
                    </div>
                </div>
            </div>

            <div class="row">
                    <div class="col-xs-12">
                        <h3>Korrigieren oder Bestätigen</h3>
                        <div>
                            <a href="" class="btn btn-warning">Korrigieren</a>
                            <a href="{{ path('event_public_participate_confirm', {'eid': event.eid }) }}?confirm" class="btn btn-success">Bestätigen</a>
                        </div>

                    </div>
                </div>
        </div>
    </div>


{% endblock %}