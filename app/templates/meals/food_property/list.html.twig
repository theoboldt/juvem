{% extends 'base.html.twig' %}

{% block title %}Eigenschaften von Lebensmitteln{% endblock %}

{% block body %}
  {% embed 'common/header/base-page-header.html.twig' with {'title': 'Eigenschaften von Lebensmitteln', 'subtitle': 'verwalten'} %}
    {% block breadcrumb %}
      <li>Rezepte</li>
      <li class="active">Eigenschaften von Lebensmitteln verwalten</li>
    {% endblock %}
  {% endembed %}
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <div id="bootstrap-table-toolbar" class="form-inline">
          <div class="form-group">
            <a class="btn btn-primary" href="{{ path('meals_property_new') }}">{{ 'plus'|glyph }} Neu</a>
          </div>
        </div>
        <table id="foodPropertyList" class="table table-striped table-remote-content" data-id-field="bid"
               data-toggle="table" data-cache="false" data-pagination="true"
               data-side-pagination="client" data-show-refresh="true" data-show-columns="true"
               data-search-align="right" data-toolbar="#bootstrap-table-toolbar" data-striped="true"
               data-show-filter="true" data-search="true"
               data-url="{{ path('meals_properties_list_data') }}">
          <thead>
          <tr>
            <th data-field="id" data-sortable="true" data-visible="false">#</th>
            <th data-field="name" data-sortable="true" data-order="asc">Titel</th>
            <th data-field="exclusion_term" data-sortable="true">Ausschluss-Begriff</th>
            <th data-field="exclusion_term_description" data-sortable="true">Ausschluss-Begriff Beschreibung</th>
            <th data-field="exclusion_term_short" data-sortable="true">Ausschluss-Begriff Abkürzung</th>
          </tr>
          </thead>
        </table>
      </div>
    </div>
  </div>
{% endblock %}
